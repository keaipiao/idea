# 01-想法记录MVP · 迭代日志（feature 级时间线）

> 对应 SOP 阶段 6 & 9 — feature 级 PR 完工索引。
> 每个 PR 的详细迭代记录在对应 PR 子目录的 `05-迭代日志.md`。
>
> 按时间倒序排列。

---

## PR 完工时间线

| 日期 | PR | 主题 | 详情 |
|---|---|---|---|
| — | — | 暂无完工 PR | — |

## 远期 PR（未启动）

- PR-6 AI 智能整理（MVP 稳定后启动）
- PR-7 搜索 + 标签（想法 > 100 条时启动）

启动时新建对应 `PR-N/` 子目录。

---

## 维护规则

- 每个 PR 完工后**仅在 PR 子目录的 `05-迭代日志.md` 写详情**（含 hotfix / 后续小修）
- 本 feature 级文档**只更新顶部表格一行**（新 PR 加一行）+ 远期 PR 启动时挪到表格
- 跨 PR 大版本里程碑（如 v1.0 GA）可在表格上方加一段总结
