# 01-想法记录MVP · PR-1 开发计划

> 本 PR 范围/风险/估时已在 feature 级 [../02-开发计划.md § 1](../02-开发计划.md) 锁定。
> 本文档是 PR-1 内部的**原子任务清单**(阶段 2.5 第二层),必须用户评审通过才能进阶段 3。

---

## 1. PR-1 概要

| 项 | 值 |
|---|---|
| 范围 | 后端脚手架 + 3 张表 + 11 个 endpoint(含 2 个 reorder) + 开发期 JWT + CORS + 输入校验 + 分页 + 项目初始化 |
| 风险 | 低(无既有依赖,无线上影响) |
| 估时 | **3 天**(实施 ~14h,加调试和文档) — plan-eng-review 加入 P0+P1 项后从 2 天扩到 3 天 |
| 回滚边界 | PR-1 前无任何代码,出问题直接 `git reset` 重来或删 `src/` |
| 上线策略 | **本 PR 不部署生产**,仅本地 dev 跑通 + 集成测试全绿即视为完成 |

---

## 2. 原子任务分解

> 标记说明:**I** = 基础设施 / 项目骨架,**B** = 后端代码,**T** = 测试与验证。
> 每条 ≤1 天,带依赖、文件路径、完成判定。

### I 任务(基础设施 + 项目骨架)

| ID | 任务 | 文件 | 依赖 | 估时 | 完成判定 |
|---|---|---|---|---|---|
| **I1** | `git init` + 根目录 `.gitignore` + `README.md` + `CHANGELOG.md` + `VERSION` | `.gitignore` / `README.md` / `CHANGELOG.md` / `VERSION` | — | 30min | `git status` 干净;`.gitignore` 含 `target/` `.env*` `.idea/` `logs/` |
| **I2** | Maven 项目骨架(`pom.xml` + 主类 + `application.yml` + `application-dev.yml` + `.env.example`) | `pom.xml` / `src/main/java/com/ideabox/api/IdeaboxApplication.java` / `src/main/resources/application.yml` / `application-dev.yml` / `.env.example` | I1 | 1.5h | `mvn compile` 通过;`mvn spring-boot:run -Dspring-boot.run.profiles=dev` 启动到 `Started`(Flyway 没跑因为 I4 还没做,允许 `DataSource` warning) |
| **I3** | `docker-compose.dev.yml`(只起 postgres-16)+ 启动脚本注释 | `docker-compose.dev.yml` | I1 | 30min | `docker compose -f docker-compose.dev.yml up -d` 成功;`docker exec ... pg_isready` 返 0 |
| **I4** | Flyway migration `V1__create_tables.sql` + `V2__seed_dev_user.sql` | `src/main/resources/db/migration/V1__create_tables.sql` / `V2__seed_dev_user.sql` | I3 | 1h | backend 启动后 `\dt` 列出 3 表;`SELECT * FROM t_user WHERE id=1` 返 1 行 |

### B 任务(后端代码)

| ID | 任务 | 文件 | 依赖 | 估时 | 完成判定 |
|---|---|---|---|---|---|
| **B1** | 包结构骨架(空 package + 主类已在 I2) | 包目录 `config/` / `common/` / `user/` / `auth/` / `project/` / `idea/`(通过创建子类带出) | I2 | 包含在 B2-B7 | — |
| **B2** | 配置类:`SecurityConfig` + `CorsConfig` + `JwtProperties` + `MybatisPlusConfig`(分页插件 + `MetaObjectHandler` autofill) + `DruidConfig` | `config/SecurityConfig.java` / `CorsConfig.java` / `JwtProperties.java` / `MybatisPlusConfig.java` / `DruidConfig.java` | I2 | 2h | 启动无报错;`/actuator/health` 200(白名单);`/api/users/me` 不带 token 返 `401 {code:401003}`;CORS preflight OPTIONS 通过(curl -H "Origin: http://localhost:3000" -X OPTIONS) |
| **B3** | 通用层:`Result` + `ResultCode`(枚举含 ADR-9 全部 code) + `BusinessException` + `GlobalExceptionHandler`(捕 `BusinessException` + `MethodArgumentNotValidException` + `Exception` 兜底) + `UserContext` + `JwtUtil` + `JwtAuthFilter`(异常捕获 → 写 Result JSON,**不抛给 Spring Security 默认 handler**) | `common/Result.java` / `ResultCode.java` / `BusinessException.java` / `GlobalExceptionHandler.java` / `UserContext.java` / `common/jwt/JwtUtil.java` / `JwtAuthFilter.java` | B2 | 2h | `JwtUtil` 签发+解析+过期+错签 4 单测全过;过期 token curl 返 `{code:401001}` JSON;错签返 `{code:401002}`;无 header 返 `{code:401003}`;`@Valid` 失败返 `{code:400001, data:[字段错误列表]}` |
| **B4** | User 模块:entity(Lombok `@Data` + `@TableField(fill)` 时间戳) + Mapper + Service(`@Transactional(readOnly=true)`) + Controller(`GET /api/users/me`) | `user/entity/User.java` / `mapper/UserMapper.java` / `service/UserService.java` / `controller/UserController.java` | B3, I4 | 1h | `GET /api/users/me` 带种子用户 JWT 返 `{nickname: '开发测试用户'}` |
| **B5** | Auth 模块:`AuthController.devLogin()`(`@Profile("dev")` 类级) + `mpLogin()` 501 占位 | `auth/controller/AuthController.java` | B3, B4 | 45min | `POST /api/auth/login/dev` 返 `{token, expiresAt, user}`;`POST /api/auth/login/mp` 返 501;prod profile 启动时 `/login/dev` 不存在 → 404 |
| **B6** | Project 模块:entity + DTO(`@NotBlank @Size(max=100)`) + VO(`fromEntity`) + Mapper + Service(`@Transactional`) + Controller(list 分页 / create / update / delete) | `project/entity/Project.java` / `dto/ProjectCreateReq.java` / `ProjectUpdateReq.java` / `ProjectVO.java` / `mapper/ProjectMapper.java` / `service/ProjectService.java` / `controller/ProjectController.java` | B4 | 2h | curl 走 list 分页 + CRUD + 跨用户 403 + DELETE 不存在 404 + name 超长 400 |
| **B7** | Idea 模块:entity + DTO(`@NotBlank @Size(max=5000)`) + VO + Mapper + Service + Controller(list 分页 + `?completed=` 过滤 / create / update / delete) | `idea/entity/Idea.java` / `dto/IdeaCreateReq.java` / `IdeaUpdateReq.java` / `IdeaVO.java` / `mapper/IdeaMapper.java` / `service/IdeaService.java` / `controller/IdeaController.java` | B6 | 2h | curl 走完成/取消完成切换 OK;`completed_at` 翻译正确;`?completed=false` 过滤未完成;content 超长 400 |
| **B8** | 批量重排接口:`PUT /api/projects/reorder` + `PUT /api/projects/{projectId}/ideas/reorder`,Service 内一次性 update sort_order(单事务 batch update) | `project/controller/ProjectController.java`(追加) / `idea/controller/IdeaController.java`(追加) / 对应 Service | B6, B7 | 1h | 传入 `{ids: [3, 1, 2]}` 后顺序变为 3→1→2;非自己的 id 混入返 403 |

### T 任务(测试 — 阶段 4)

| ID | 任务 | 文件 | 依赖 | 估时 | 完成判定 |
|---|---|---|---|---|---|
| **T1** | Service 层单测(`UserService` / `ProjectService` / `IdeaService` / `JwtUtil`) | `src/test/java/com/ideabox/api/**/*ServiceTest.java` | B4-B7 | 2h | 所有正常/异常分支覆盖,全绿 |
| **T2** | 集成测试 + JWT 端到端(`@SpringBootTest` + Testcontainers PG) | `src/test/java/com/ideabox/api/AuthIntegrationTest.java` / `ProjectIntegrationTest.java` / `IdeaIntegrationTest.java` / `ReorderIntegrationTest.java` | B5-B8 | 3h | **金路径**:登录→token→创建项目→创建想法→完成想法→重排;**NEG 6 case 全过**:① 过期 JWT 返 `{code:401001}` ② 错签 JWT 返 `{code:401002}` ③ 无 Authorization 返 `{code:401003}` ④ 跨用户 GET/PUT/DELETE 返 `{code:403001}` ⑤ DELETE 不存在 id 返 `{code:404001}` ⑥ name 超长 / content 超长 / name 空白返 `{code:400001}` |
| **T3** | `/cso` 安全扫描 | — | T1, T2 | 1h | 0 P0 / 0 P1(必跑条件:鉴权变更 + 用户输入→DB + 新依赖 + CORS,全部命中) |

### V 任务(阶段 5 真实启动验证)

| ID | 任务 | 完成判定 |
|---|---|---|
| **V1** | dev profile `mvn spring-boot:run` + actuator 健康 | `/actuator/health` 200 UP;启动日志 `Started IdeaboxApplication in <30s` |
| **V2** | curl 端到端金路径 | 1) `POST /api/auth/login/dev` → token<br>2) `GET /api/users/me` → 用户<br>3) `POST /api/projects` → 项目<br>4) `POST /api/projects/{id}/ideas` → 想法<br>5) `PUT /api/ideas/{id}` → 完成<br>6) `GET /api/projects/{id}/ideas` → completed_at 有值 |
| **V3** | 边界 ≥2 个 | 1) 不带 token 访问 `/api/projects` 返 401<br>2) 用户 A 的 token 访问用户 B 的 project 返 403<br>3) Druid `/druid/login.html` 仅 localhost 可达 |
| **V4** | 全量测试回归 | `mvn test` 全绿,0 fail |

### 任务依赖图

```
I1 ─→ I2 ─→ I3 ─→ I4 ─┐
       │              │
       └→ B2 ─→ B3 ─→ B4 ─→ B5
                       │
                       └→ B6 ─→ B7 ─→ B8
                                     │
                                     └→ T1 ─→ T2 ─→ T3 ─→ V1 ─→ V2 ─→ V3 ─→ V4
```

---

## 3. 回滚边界

- **代码回滚**:PR-1 之前项目无代码,出问题 `rm -rf src/ pom.xml target/` 重来
- **数据回滚**:本地 dev DB,`docker compose down -v` 清掉
- **本 PR 不部署生产**,无生产回滚问题

---

## 4. 工程评审结论(plan-eng-review 2026-05-28)

**初次评审维度分**(满分 10):
- 架构 8 / 数据模型 8 / 接口契约 7 / 安全 7 / 测试覆盖 6 / 性能 8 / 边界场景 5 / 可维护性 8 / 部署回滚 9

**修订后维度分**(全部 ≥8):
- 架构 9(加 ADR-12 事务 + ADR-13 DTO 转换)
- 数据模型 9(加 ADR-15 MetaObjectHandler 自动填充)
- 接口契约 9(加 ADR-9 状态码映射 + ADR-16 分页 + reorder)
- 安全 9(加 ADR-10 CORS + ADR-11 输入校验 + JwtAuthFilter 异常捕获)
- 测试覆盖 9(T2 加 6 个 NEG case)
- 性能 8(MVP 数据量小 + 分页就绪)
- 边界场景 9(404/403/401 三档 + 参数校验全覆盖)
- 可维护性 9(加 ADR-14 Lombok + ADR-13 手写 DTO 转换约定)
- 部署回滚 9

**采纳 ADR 净增**:ADR-9 到 ADR-16,共 8 条。

**仍 deferred 进 § 6 TODO**:JWT refresh / revocation、API rate limit、OpenAPI/Swagger、XSS sanitize、跨时区策略。

**Outside Voice / Codex Review**:Codex CLI 在当前环境不可用,跳过。

---

## 5. 时间线

| 日期 | 里程碑 | 状态 |
|---|---|---|
| 2026-05-28 | 阶段 2.5 第二层 PR-1 任务清单定稿 | 🔄 |
| 待定 | 阶段 3 实施完成(B1-B7)+ `/review` PASS | ⏸ |
| 待定 | 阶段 4 测试完成(T1-T3)+ `/cso` 0 P0/P1 | ⏸ |
| 待定 | 阶段 5 真实启动验证(V1-V4)| ⏸ |
| 待定 | 阶段 7 merge 前 `/review` + `/cso` 双跑 | ⏸ |
| 待定 | 阶段 9 归档 | ⏸ |

---

## 6. 后续优化清单(实现过程中陆续追加)

**已知 deferred(plan-eng-review 显式延后)**:

- [ ] **短期**:`@Async` 引入时需要把 `UserContext` 升级为 `TransmittableThreadLocal`
- [ ] **短期**:Refresh token + JWT revocation(若 7 天过期成实际痛点 / 用户改密场景)
- [ ] **短期**:OpenAPI/Swagger UI(PR-2/3 启动时如果接入成本 < 1h 再做)
- [ ] **中期**:API rate limit(`bucket4j` 类),`/api/auth/login/dev` 优先
- [ ] **中期**:XSS sanitize — 当前 content 纯文本前端只读不渲染 HTML,接受不 sanitize;若未来引入富文本就必须做
- [ ] **中期**:操作审计日志表(用户 id × 资源 × 动作 × 时间)
- [ ] **中期**:跨时区策略 — DB 用 `TIMESTAMP`(无时区)+ JVM Asia/Shanghai,跨时区客户端通过 ISO-8601 + 前端本地化兜底
- [ ] **长期**:`sort_order` 重排稀疏化(避免高频拖拽全表 reindex,用 1024 步长 + 失衡时 rebalance)
