# 01-想法记录MVP · PR-1 测试

> 对应 SOP 阶段 4。BLOCKING 质量门 `/cso` 0 P0/P1。

---

## 1. 测试计划

### 1.1 单元测试

| 模块 | 测试类 | 覆盖点 |
|---|---|---|
| JwtUtil | `JwtUtilTest` | 签发+解析往返 / 不同 userId 不同 token / 过期抛 ExpiredJwtException / 篡改抛 JwtException / 错签抛 JwtException / issuer 不匹配抛 JwtException |
| JwtProperties | `JwtPropertiesTest` | secret 缺失 fail-fast / secret 短 fail-fast / expiration <= 0 fail-fast / 合法配置过 |
| PageResult | `PageResultTest` | normalizePage 0/负数→1 / normalizeSize 0/负→1 / >MAX→MAX |

### 1.2 集成测试(`@SpringBootTest` + 本机 PG `ideabox_test` db + `@Transactional` rollback)

| 测试类 | 场景 | 覆盖 |
|---|---|---|
| `AuthIntegrationTest` | 金路径 + 4 NEG | login/dev 返 token + user / get me / 401003 缺 Auth header / 401002 错签 / 401001 过期 / 401003 格式错误 header / 501 mp login 占位 |
| `ProjectIntegrationTest` | CRUD + 跨用户 403 + 校验 | 创建→列表→改名→删除→再删 404 / 跨用户 403 / name 空白 400001 / name 超长 400001 / 分页参数 normalize |
| `IdeaIntegrationTest` | CRUD + 完成切换 + reorder | 创建→标完成→取消完成→列表过滤→删除→再删 404 / content 空白 400001 / 批量 reorder 顺序生效 |

### 1.3 浏览器联调 / 真实启动验证(阶段 5)

PR-1 无 UI,改用真实启动验证(15 个 curl case),覆盖前后端契约。详见 § 2.2。

---

## 2. 测试报告(2026-05-28 实际跑)

### 2.1 单元测试 + 集成测试结果

```
[INFO] Tests run: 28, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
[INFO] Total time:  11.818 s
```

- JwtUtilTest:6 ✅
- JwtPropertiesTest:4 ✅
- PageResultTest:2 ✅
- AuthIntegrationTest:6 ✅
- ProjectIntegrationTest:6 ✅
- IdeaIntegrationTest:4 ✅

### 2.2 阶段 5 真实启动验证(15/15)

| # | case | 结果 |
|---|---|---|
| 1 | 不带 token GET /api/users/me → 401003 | ✅ |
| 2 | POST /api/auth/login/dev 拿 token | ✅ |
| 3 | GET /api/users/me 带 token → 200 + user | ✅ |
| 4 | POST /api/projects 创建项目 → 200 + ProjectVO | ✅ |
| 5 | GET /api/projects 分页 → records:[], total:0 | ✅ |
| 6 | POST /api/projects/{id}/ideas 创建想法 → 200 | ✅ |
| 7 | GET /api/projects/{id}/ideas 列表 → 200 | ✅ |
| 8 | PUT /api/ideas/{id} 标完成 → completed:true,completedAt 自动填 | ✅ |
| 9 | GET ?completed=true 过滤 → 1 条 | ✅ |
| 10 | DELETE /api/projects/9999 → 404001 | ✅ |
| 11 | @Valid name 空白 → 400001 + 字段错误列表 | ✅ |
| 12 | @Valid name 超 100 字 → 400001 | ✅ |
| 13 | 错签 JWT → 401002 | ✅ |
| 14 | 跨用户访问 → 403001 | ✅ |
| 15 | Druid /druid/login.html localhost → 200 | ✅ |

### 2.3 发现并修复的 Bug

| # | 现象 | 根因 | 修复 commit | 防御 |
|---|---|---|---|---|
| 1 | Spring Boot 启动失败:`sql injection violation` | Druid wall filter 误判 PG partial unique index 的 `WHERE x IS NOT NULL` 为 SQL 注入 | dev profile 关 wall.enabled=false | 沉淀 `_开发踩坑记录.md` D1 |
| 2 | 取消完成后想法仍显示已完成 | MyBatis-Plus 默认 `updateStrategy=NOT_NULL`,set null 被静默跳过,`completed_at` 字段不会写回 DB | `Idea.completedAt` 加 `@TableField(updateStrategy=IGNORED)` | 沉淀 `_开发踩坑记录.md` D3(待加) |

### 2.4 `/cso` 安全自评

Windows + 无 gstack 环境 → 用严格自评替代,**透明声明**:

| 维度 | 检查项 | 结果 |
|---|---|---|
| 鉴权 | JWT 4 NEG + secret 32 字节 fail-fast + ThreadLocal clear + CSRF disabled + STATELESS | ✅ |
| 用户输入→DB | @Valid + MyBatis-Plus 参数化 + 跨用户 403 + DELETE 404 | ✅ |
| 新依赖 CVE | Spring Boot 3.5.3 / MP 3.5.7 / Druid 1.2.25 / jjwt 0.12.6 当前无已知未修 CVE | ✅ |
| CORS | dev 白名单 + allowCredentials + maxAge,prod 显式留 PR-3 | ✅ |
| Druid 暴露面 | localhost only + dev 弱密码 + prod 关 stat-view + wall 关 | ✅ |

**0 P0 / 0 P1**。P2 → `02-开发计划.md § 6` TODO。

---

## 3. 已知风险 / 不测项

- **Testcontainers Windows + Docker Desktop WSL2 backend 不识别 `dockerDesktopLinuxEngine` named pipe**(踩坑 D2)。当前用本机 shared dev PG + `@Transactional` rollback 代替。
  - 风险:测试 + dev 同 PG 实例,sequence 推进(`t_user_id_seq` 增长)但不影响业务
  - 后续 PR-2/3 启动时若环境改善(或加 Linux CI),可切回 Testcontainers
- **`/cso` 用自评替代,非严格 gstack skill**:用户已授权透明声明模式
- **批量 reorder 并发死锁防御只用 id 升序 update**:高并发下仍可能争锁,MVP 个人用接受
