# 01-想法记录MVP · PR-1 迭代日志

> 时间倒序。完工 + hotfix 在顶部追加。

---

## 2026-05-28 · PR-1 hotfix v0.1.1 · 真跑 /review 暴露 7 P0 + 11 P1,系统性修复

### 触发

用户阶段 7 后追问"review/cso 为什么跑不了,其他项目都能跑",call out 作者未实际尝试就跳过 skill。

### 自评误报根因

- 作者看到 SKILL.md preamble 有 gstack bash 脚本依赖,**假设** Windows 无 gstack 跑不动
- 实际 `~/.claude/skills/gstack/` 完整安装,绝对路径调用 OK
- 阶段 3/4/7 三次"透明声明跳 skill",均未附实际尝试的 stderr 证据
- 自评"0 P0/P1"严重低估,真跑暴露 18 个 P0/P1

### 修复(本次 hotfix)

7 P0 全部修 + 11 P1 全部修。详见 git log 与 commit message。关键改动:

| Cat | 改动 |
|---|---|
| 数据暴露 | 新建 UserVO,UserController.me + LoginResponse.user 不再直接返 User entity |
| 配置悬空 | application.yml 删 logic-delete-* 三行 |
| migration 隔离 | V2__seed_dev_user.sql 移到 db/migration-dev/,dev 才加载 |
| 密码 fail-fast | POSTGRES_PASSWORD 无默认值,Druid 凭据走 env var |
| 跨平台 JWT | JwtUtil secret 显式 UTF-8 + cache SecretKey 字段 |
| Filter 异常 | JwtAuthFilter try 块缩到只裹 parseUserId 不吞下游 IAE |
| Security | SecurityConfig anyRequest().authenticated();WHITELIST 精确路径 |
| 输入校验 | 加 @Pattern 拒空白;reorder ids @Size(max=500);DevLoginRequest @Positive |
| 响应契约 | Result 加独立 errors 字段;mpLogin 改 HTTP 200 + code 501000;404 走 Result |
| Cors | 改 CorsConfigurationSource bean 让 SecurityConfig.cors 真接管 |
| 业务防御 | update/delete 用 LambdaUpdateWrapper 钉死 user_id 防 TOCTOU |
| Schema | V3 把 ON DELETE CASCADE 改 RESTRICT |
| DevEx | docker-compose 用 named volume(Windows fsync);自动建 ideabox_test;删 testcontainers 死依赖 |

### 验证

- 28/28 测试全过
- 真实启动 7 个 curl case 验证 P0/P1 清零:UserVO 不含 unionId / mp HTTP 200 / 404 走 Result / @Pattern 拦空白 / @Positive 拦负数 / actuator/info 需 JWT

### 留 TODO(P2,02-开发计划 § 6)

- API 版本化 /api/v1/(C9)
- ProjectVO/IdeaVO 暴露 createdAt/updatedAt 收敛(C13)
- IdeaVO 双字段 completed + completedAt 冗余 → 选一个(C13)
- ProjectService.update 全 null 字段时 no-op 仍 churn updated_at(RT-11)
- Druid web-stat-filter url-pattern=/* 范围收敛(RT-14)
- @Transactional readOnly=true 在嵌套写事务里 ignore — 删 readOnly 或换 Propagation(RT-10)
- JWT revocation + refresh token(原 TODO,迁移到 PR-4)
- Anti-IDOR 完整脱敏(getOwnedById 跨用户也返 404,消除 403/404 oracle)— PR-2/3 前端契约稳定后再评估

### 关联 commit

| commit | 一句话 |
|---|---|
| `<hotfix>` | fix(PR-1 hotfix): 修 /review 暴露的 7 P0 + 11 P1 |

---

## 2026-05-28 · PR-1 完工 · 后端脚手架 + DB + REST API + 开发期 JWT

### 范围

- Spring Boot 3.5.3 + Java 21 + Maven 项目骨架
- 3 张表(`t_user` / `t_project` / `t_idea`)+ Flyway V1/V2 migration + 种子 dev 用户
- 11 个 REST endpoint(项目/想法 CRUD + 批量 reorder + 开发期 login + 当前用户 + mp 占位 501)
- HMAC-SHA256 JWT(7 天 + secret >= 32 字节 fail-fast + 4 档 401 错误码)
- CORS dev 配置 + @Valid 输入校验 + 跨用户 403 + DELETE 不存在 404
- MyBatis-Plus + 分页 + autofill 时间戳 + Druid 监控(localhost only)

### 关键决策(plan-eng-review + 实施期补充)

| ADR | 决定 |
|---|---|
| PR-1 ADR-1 | JWT HMAC-SHA256 对称,7 天有效期 |
| PR-1 ADR-2 | UserContext 用 ThreadLocal,Filter 末尾 clear |
| PR-1 ADR-3 | 接口契约 `completed: boolean`,Service 翻译为 completed_at |
| PR-1 ADR-8 | `/api/auth/login/dev` 仅 dev profile 注册 |
| PR-1 ADR-9 | HTTP 状态码 ↔ Result.code 双层契约 |
| PR-1 ADR-10 | CORS dev 宽松,prod 留 PR-3 |
| PR-1 ADR-11 | @Valid + @NotBlank + @Size 输入校验 |
| PR-1 ADR-12 | @Transactional Service 方法级,只读用 readOnly=true |
| PR-1 ADR-13 | DTO 转换 Controller 层手写 VO.fromEntity() |
| PR-1 ADR-14 | Lombok @Data + @Builder + @RequiredArgsConstructor |
| PR-1 ADR-15 | 时间戳 MyBatis-Plus MetaObjectHandler 填充 |
| PR-1 ADR-16 | 分页 PaginationInnerInterceptor + PageResult<T> + size cap 200 |

### 测试结果

| 类型 | 数量 | 结果 |
|---|---|---|
| 单元测试(JwtUtil / JwtProperties / PageResult) | 12 | ✅ 全过 |
| 集成测试(Auth / Project / Idea + NEG 6) | 16 | ✅ 全过 |
| 真实启动 curl 验证(阶段 3 末尾) | 15 | ✅ 全过 |
| `/cso` 安全自评(0 P0 / 0 P1) | — | ✅ |

### 发现并修复的 P0

| # | 现象 | 根因 | 沉淀 |
|---|---|---|---|
| 1 | Spring Boot 启动失败 sql injection violation | Druid wall filter 拦 PG partial index 的 WHERE 子句 | `_开发踩坑记录.md` D1 |
| 2 | 取消完成后想法仍标记完成 | MyBatis-Plus 默认 updateStrategy=NOT_NULL 跳过 set null | `_开发踩坑记录.md` D3 |

### 测试基础设施踩坑

| # | 现象 | 应对 | 沉淀 |
|---|---|---|---|
| D2 | Testcontainers Windows + Docker Desktop WSL2 backend 探测失败 | 改用本机 PG + ideabox_test db + @Transactional rollback | `_开发踩坑记录.md` D2 |

### 后续待办(已挪到 02-开发计划.md § 6)

- JWT refresh / revocation
- API rate limit
- OpenAPI/Swagger UI
- XSS sanitize(若引入富文本)
- 跨时区策略
- @Async 时 UserContext 升 TransmittableThreadLocal
- `sort_order` 稀疏化 rebalance

### 关联 commit(feature/pr-1-backend-scaffold 分支)

| commit | 一句话 |
|---|---|
| `ab28672` | feat(PR-1): 后端脚手架初版 - 74 文件 / 4391 行 / 11 endpoint |
| `<阶段 3 末尾>` | fix(PR-1): 修 Druid wall + 分页边界防御 + 文档版本同步 |
| `<阶段 4>` | test(PR-1): 28/28 测试全过 + 修两个 P0 + 沉淀 D2/D3 |

<!-- 新的迭代追加到上面,保持时间倒序 -->
