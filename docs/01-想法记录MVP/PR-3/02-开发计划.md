# 01-想法记录MVP · PR-3 开发计划

> 本 PR 范围/风险/估时已在 feature 级 [../02-开发计划.md § 1](../02-开发计划.md) 锁定。
> 本文档是 PR-3 内部的**原子任务清单**(阶段 2.5 第二层),必须用户评审通过才能进阶段 3。
> 设计依据:[01-设计.md](01-设计.md)。

---

## 1. PR-3 概要

| 项 | 值 |
|---|---|
| 范围 | Next.js 16 + TS + Tailwind 4 网页端:AuthGate / ProjectSidebar / IdeaInput / IdeaList(可拖拽)/ IdeaItem(双击编辑 Modal,hover 右上角删除 +)/ EmptyState / Toast Provider / Modal Root / Skeleton + SWR cache + Noto Sans SC self-host + XSS mitigation + nginx 反代 + Playwright e2e + 2 路由 + dev login + a11y 最小集 |
| 风险 | 中(首次前端 + 拖拽手势冲突 + Tailwind v4 / Next 16 较新 + 五件叠加 Windows 首次跑通) |
| 估时 | **5-6 天**(plan-eng-review 16 项修法追加从 4 天到 5-6 天;实施 ~28h + 联调 ~5h + 测试调试 ~6h) |
| 回滚边界 | `web/` 独立目录,出问题 `rm -rf web/` 不影响后端;部署期回滚 `docker compose up -d web` 上一 tag |
| 上线策略 | dev 跑通 + 单测 + 联调 0 console error + `/review` + `/cso` 双门通过 → 本机 prod stack 预演 → 合主分支(本 PR 暂不部署生产服务器,等 PR-4 微信登录就绪一并上线) |

---

## 2. 原子任务分解

> 标记说明:**I** = 基础设施 / 项目骨架,**F** = 前端代码,**T** = 测试,**V** = 联调验证。
> 每条 ≤1 天,带依赖、文件路径、完成判定。

### I 任务(基础设施 + 项目骨架)

| ID | 任务 | 文件 | 依赖 | 估时 | 完成判定 |
|---|---|---|---|---|---|
| **I1** | `cd <repo root> && npx create-next-app@latest web --typescript --tailwind --app --src-dir false --import-alias '@/*' --turbopack`(交互式选 yes/no) | `web/{package.json,next.config.ts,tsconfig.json,tailwind.config.ts,postcss.config.js,app/layout.tsx,app/page.tsx,app/globals.css}` | — | 30min | `cd web && npm run dev` 启动 `http://localhost:3000` 显示 Next.js 默认页 |
| **I2** | Tailwind v4 配置切换 + design token 镜像 `_UI设计规范.md` | `web/app/globals.css`(`@import "tailwindcss"; @theme { --color-primary: #1677FF; ... }`) / `web/postcss.config.js` | I1 | 45min | `<div className="text-[var(--color-primary)]">` 渲染蓝色;DevTools `:root` 看到 8 色 + 3 圆角 + 5 间距 + 字体栈全部 token |
| **I3** | 文案常量 + API 类型 + ResultCode 镜像 | `web/lib/copy.ts`(toast / placeholder / EmptyState 文案)/ `web/lib/types.ts`(ProjectVO / IdeaVO / UserVO / LoginResponse / Result<T> / PageResult<T> / ApiError class) | I1 | 1h | `tsc --noEmit` 0 error;所有文案集中无字符串散落组件 |
| **I4** | Dockerfile multi-stage(三阶段 + 3 处 COPY)+ `.env.example` + `.env.local`;**修 PR-1 端口 5432 → 25432** | `web/.env.example`(`NEXT_PUBLIC_API_BASE_URL=http://localhost:8080` + `NEXT_PUBLIC_AUTH_MODE=dev`)/ `web/Dockerfile`(`output: 'standalone'`,见 § 4.7)/ 根 `docker-compose.dev.yml`(PG `25432:5432`)/ `src/main/resources/application-dev.yml`(`url: jdbc:postgresql://localhost:25432/ideabox`)/ 根 `.env.example`(`POSTGRES_PORT=25432`) | I1 | 2h | `npm run dev` 启动 OK;`docker build --build-arg NEXT_PUBLIC_API_BASE_URL=http://host.docker.internal:8080 --build-arg NEXT_PUBLIC_AUTH_MODE=dev -t ideabox-web ./web` 构建成功;**`docker run -p 3000:3000 ideabox-web` 访问后 `curl http://localhost:3000/_next/static/<chunk>.css` 200**(防 standalone 漏拷);PG 改 25432 端口后 backend `mvn spring-boot:run` 仍能连 |
| **I4a** | nginx 反代 config + `docker-compose.prod.yml` 加 web + nginx service(Outside voice C) | `docker/nginx/nginx.conf`(/ → web:3000,/api → backend:8080,CSP header)/ `docker-compose.prod.yml`(web service + nginx service) | I4 | 1.5h | 本机 prod stack `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build` 起来后 `curl http://localhost/` 200(经 nginx),`curl http://localhost/api/actuator/health` 200,响应 header 含 `Content-Security-Policy` |
| **I5** | 引入运行依赖:`@dnd-kit/core` `@dnd-kit/sortable` `@dnd-kit/utilities` + `swr` + Vitest + Playwright + `next/font/google` Noto Sans SC + ESLint plugin `react/no-danger` 规则 | `web/package.json` / `web/package-lock.json` / `web/eslint.config.mjs` | I1 | 1h | `npm ls @dnd-kit/core swr vitest @playwright/test` 全显示;`tsc` 0 error;`npm run lint` 0 error;`eslint --print-config` 显示 `react/no-danger: error` |

### F 任务(前端代码)

| ID | 任务 | 文件 | 依赖 | 估时 | 完成判定 |
|---|---|---|---|---|---|
| **F1** | API 客户端 fetch 包装 + 401 一次性 re-login + **单测同步随写**(ADR-21) | `web/lib/api.ts`(`api<T>` + `ApiError` + `dispatchAuthRefresh`)+ `web/__tests__/lib/api.test.ts`(re-login 死循环防御 / timeout / 4xx / 5xx / code≠0 / 401 三档分支)| I3, I5 | 3.5h | `npm run test api.test` 全绿;F1 完成 ≠ 仅代码,**必须含 5+ test case**;Authorization header 自动加;**re-login 最多 1 次,第二次 401 立即 throw**;abort/timeout 触发 ApiError |
| **F2** | dev login + token 持久化 + **`NEXT_PUBLIC_AUTH_MODE` 分支**(ADR-16) + idle 30min 自动 clearToken(ADR-18) | `web/lib/auth.ts`(`getToken() / setToken() / clearToken() / devLogin() / ensureAuth(mode) / useIdleTimer(30*60*1000)`)+ `web/app/_components/AuthGate.tsx`(按 mode 分支:`dev` → devLogin,`wechat` → 占位"PR-4 微信扫码",`none` → 跳登录页)+ `web/__tests__/lib/auth.test.ts` | F1 | 2h | dev mode 首次自动 login + token 写 localStorage;`AUTH_MODE=wechat` 不自动 login;idle 30 分钟无操作自动 clearToken;3 个 mode 分支单测全过 |
| **F3** | useProjects hook **改用 SWR**(ADR-15)+ ProjectSidebar 组件 | `web/app/_hooks/useProjects.ts`(`useSWR(['projects', uid], fetcher)` + mutate)/ `web/app/_components/ProjectSidebar.tsx`(列表 + 新建 input + 当前选中样式 `aria-current="page"` + 长按弹重命名 / 删除菜单) | F1, F2, I3, I5 | 2.5h | 列出项目列表;切路由后 SWR cache 命中 0 重 fetch;`mutate(['projects',uid], data, {revalidate:false})` 乐观更新;点击触发 `router.push('/projects/{id}')`;新建项目 toast "已记录";长按弹菜单可改名 / 删 |
| **F4** | useIdeas hook **改用 SWR**(ADR-15)+ IdeaList + EditIdeaModal 独立组件(ADR-19) + IdeaItem | `web/app/_hooks/useIdeas.ts`(`useSWR(['ideas', projectId])`)/ `web/app/_components/IdeaList.tsx`(未完成区 + 已完成区折叠分隔)/ `web/app/_components/EditIdeaModal.tsx`(独立组件,Modal portal 渲染)/ `web/app/_components/IdeaItem.tsx`(○ 完成 / 双击触发 `openModal(<EditIdeaModal>)` / hover 右上角 × 删除 / 删除 toast undo) | F1, F2, F3, I3, I5 | 3h | 列出想法;切项目 SWR cache;点 ○ 切换完成;双击卡片正文 `useModal().open(<EditIdeaModal>)`;hover 显示右上角 × 删除按钮;点删除 toast "已删除 [撤销]" 5s;Esc 关 Modal;**想法内容 ONLY textContent / `{content}`**,无 markdown 解析(ADR-18) |
| **F5** | IdeaInput 组件 + 乐观更新 + 字数计数 + over-limit / no-project disabled | `web/app/_components/IdeaInput.tsx` | F1, F4, I3 | 2h | Enter 发送清空输入;Shift+Enter 换行;立刻插入占位卡片(opacity 0.6 + spinner);成功实化;失败变红边框 + 重试;> 5000 字计数变红 + disabled;未选项目时 placeholder "请先选择项目" + disabled |
| **F6** | SortableIdeaList **双 SortableContext 独立 wrap**(ADR-14 H 修法) + useIdeaReorder hook 加 **debounce 300ms**(ADR-17) + IdeaItem 拖拽手柄 + 同区域约束 | `web/app/_components/SortableIdeaList.tsx`(`<DndContext sensors={[PointerSensor(activationConstraint: {distance:8}), KeyboardSensor]}>` 内含两个独立 `<SortableContext items={undoneIds}>`(`useMemo` 稳定引用)+ `<SortableContext items={doneIds}>`)<br>`web/app/_hooks/useIdeaReorder.ts`(乐观 update SWR cache 立即 + debounce 300ms 合并 `PUT /ideas/reorder` + 失败 revert + toast) | F4, I5 | 3.5h | 拖动手柄移卡片;松手立即更新 SWR cache;**连续 5 次快速拖只发 1 次 reorder**(debounce);失败 revert + toast "排序失败";已完成 / 未完成区彼此**真不能跨拖**(双 context 隔离,非 if-check 伪造);键盘 Tab 到手柄按 Space 进入键盘排序模式 |
| **F7** | EmptyState + **ToastProvider Context**(ADR-20) + **ModalRoot portal**(ADR-19) + Skeleton(200ms-5s) | `web/app/_components/EmptyState.tsx` / `web/app/_components/ToastProvider.tsx`(`role="status" aria-live="polite"` + Context + `useToast()` hook + FIFO max 3) / `web/app/_components/ModalRoot.tsx`(React portal + `useModal()` hook + 同时刻 1 个 + Esc 关) / `web/app/_components/Skeleton.tsx` | I3 | 2h | 空项目 EmptyState 渲染;`useToast` 在任意组件可调,5 toast FIFO max 3 同时显;`useModal` 任意组件可弹 modal,同时多次 open 第二个等待;Esc 关 modal;< 200ms 加载 skeleton 不闪;超 5s 触发 error toast |
| **F8** | 路由 + 页面装配 + **Noto Sans SC self-host**(ADR-22) + ModalRoot + ToastProvider 顶层包裹 | `web/app/layout.tsx`(html lang=zh-CN + `import { Noto_Sans_SC } from 'next/font/google'` + global.css + `<ToastProvider>` + `<ModalRoot>` 包 children) / `web/app/page.tsx`(`'use client'` + AuthGate + 重定向第一个项目) / `web/app/projects/[id]/page.tsx`(双栏布局 + AppBar mobile) / `web/app/globals.css`(`@theme { --font-sans: var(--font-noto-sc), "PingFang SC", "Microsoft YaHei", system-ui, sans-serif; ... }`) | F2-F7 | 2.5h | 桌面 ≥ 1024px 双栏;`md` 768-1023 侧栏图标化;`< 768` 单栏 + AppBar + 抽屉切项目;路由切换平滑;`/` 自动跳第一个项目;**字体加载 swap fallback,FOIT 不出现**;Chrome / Edge / Firefox / Safari 字体一致 |
| **F9** | a11y 最小集补强 + AI Slop grep 自检 + **XSS lint 检查**(ADR-18) | 各组件 + `web/scripts/lint-design-tokens.sh`(grep `from-(red|blue|...)-\d+` / `text-(red|gray|...)-\d+` / `rounded-(xl|2xl|3xl)` 0 命中)+ ESLint config `react/no-danger: error` 已在 I5 设;补 `web/scripts/lint-xss.sh`(grep `dangerouslySetInnerHTML` 0 命中) | F2-F8 | 1.5h | Lighthouse a11y ≥ 90;键盘 Tab 顺序正确;`lint-design-tokens.sh` 0 violation;`lint-xss.sh` 0 violation;`npm run lint` 0 error |

### T 任务(测试 — 阶段 4)

| ID | 任务 | 文件 | 依赖 | 估时 | 完成判定 |
|---|---|---|---|---|---|
| **T1** | 单元测试(Vitest + jsdom):**F1/F2 已随写**(api.ts + auth.ts);**本任务补 4 个 path**(plan-eng-review 3.1):re-login 死循环 / timeout / abort / `useToast` 队列 / `useIdleTimer` / SWR cache key + revalidate + mutate;`lib/copy` 完备性 | `web/__tests__/hooks/useIdeas.test.tsx` / `useProjects.test.tsx` / `useToast.test.tsx` / `useIdleTimer.test.tsx` 等;F1/F2 测试已在 F1/F2 随写 | F1-F7 | 2h | `npm run test` 全绿;cover:re-login max 1 次防死循环 / fetch timeout abort / AUTH_MODE 3 分支 / SWR cache revalidate-on-focus / mutate 乐观更新 / Toast FIFO max 3 |
| **T2** | 组件交互测试(Vitest + `@testing-library/react`):IdeaInput Enter 发送 + 乐观插入回滚 / IdeaItem 双击编辑 / ○ 切换完成 / 删除 toast undo + 点 undo 不调 DELETE / over-limit 字数变红 / Modal Esc 关 | `web/__tests__/components/*.test.tsx` | F4-F7 | 2h | 全绿;`@testing-library` query 走 a11y role / label(不走 className) |
| **T3** | 拖拽集成测试(`@dnd-kit` 测试 utilities)+ a11y 键盘排序 + debounce 验证 | `web/__tests__/components/SortableIdeaList.test.tsx` | F6 | 1.5h | Tab 到手柄按 Space 进入键盘模式,↑↓ 切位置,Space 确认 = 触发 reorder API;**连续 fireEvent 5 次拖只触发 1 次 mutate**(debounce 300ms);两个 SortableContext 独立 — undone 区拖动不影响 done 区 |
| **T4** | `/cso` 安全扫描 | — | T1-T3, T5 | 1h | 0 P0 / 0 P1(必跑条件:用户输入 → API + localStorage token + 新依赖 @dnd-kit/swr + 跨域 fetch + CSP header,全部命中) |
| **T5** | **Playwright e2e 5 case**(plan-eng-review 3.2) | `web/e2e/golden-path.spec.ts`(case 1 完整金路径)/ `web/e2e/token-expiry.spec.ts`(case 2 token 失效 re-login)/ `web/e2e/dnd.spec.ts`(case 3 鼠标拖 + case 4 键盘排序)/ `web/e2e/error-states.spec.ts`(case 5 backend down 时 toast);`web/playwright.config.ts`(单 Chromium worker,baseURL http://localhost:3000) | F1-F9 + dev stack 启动 | 6h | 5 case 全绿;CI 可重复跑(headless);失败截图自动保存 `web/e2e/screenshots/` |

### V 任务(阶段 5 真实浏览器联调)

| ID | 任务 | 工具 | 完成判定 |
|---|---|---|---|
| **V1** | 启 dev stack 真跑 | `docker compose -f docker-compose.dev.yml up -d`(PG)+ `mvn spring-boot:run -Dspring-boot.run.profiles=dev`(backend)+ `cd web && npm run dev`(frontend) | 三个进程都 ready,`curl http://localhost:8080/actuator/health` UP,`curl http://localhost:3000` 200 |
| **V2** | Claude in Chrome MCP 金路径联调 | `navigate('http://localhost:3000')` + 系列点击 / 输入 | 1) AuthGate 自动 dev login,localStorage 看到 token<br>2) 新建项目"测试 A" → toast<br>3) 选中项目,IdeaInput 输入"想法 1" + Enter → 卡片插入(看到乐观占位 → 实化)<br>4) 再输入"想法 2"<br>5) 点 ○ 完成"想法 1" → 滑到已完成区<br>6) 双击"想法 2" 弹 Modal,改内容 + 保存<br>7) hover"想法 2" 右上角 × → 点删除 → toast undo<br>8) 拖拽"想法 2"换位 → 顺序变,刷新页面顺序保留<br>9) 切到桌面 1024px / 平板 768px / 移动 375px 三档布局正确<br>10) console 0 error / network 0 红色 4xx/5xx |
| **V3** | 边界 + 错误路径 | — | 1) 强制 token 失效(localStorage 改坏)→ 自动 re-login 一次<br>2) backend 停掉时网络 toast "网络异常"<br>3) > 5000 字 IdeaInput 字数计数变红 disabled<br>4) 删除后立即 undo → 卡片恢复 + 不调真 DELETE<br>5) 拖拽时网络失败 → 顺序 revert + toast |
| **V4** | a11y 联调 | Chrome DevTools Lighthouse + axe DevTools extension(若有)+ 手动键盘 nav | Lighthouse a11y ≥ 90;Tab 全程不卡 + 焦点环可见;screen reader DOM 顺序合理 |
| **V5** | 真 prod stack 预演(5 子判定,Outside voice I) | `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build` + 系列 curl | **V5.1** prod 镜像 build 成功:`docker images ideabox-web:latest` ≤ **200MB**(§ 2.1 承诺)<br>**V5.2** 静态资源未漏拷:`curl http://localhost/_next/static/<chunk-hash>.css` HTTP 200 + Content-Type css(防 standalone 漏 COPY)<br>**V5.3** NEXT_PUBLIC_* build-time inline 正确:`curl http://localhost/ -o page.html; grep -o "API_BASE_URL=[^\"]*" page.html` 显示 prod URL(非 localhost)<br>**V5.4** nginx 反代生效:`curl http://localhost/api/actuator/health` 200 UP(经 nginx 80 → backend 8080);CSP header 显示<br>**V5.5** PID 1 信号处理:`docker compose stop web` 在 5s 内退出(SIGTERM 优雅退出,非 10s 强杀) |

### 任务依赖图

```
I1 ─→ I2 ─→ I3 ─→ I4 ─→ I4a ─┐
                              │
       └→ I5 ──────────────────┤
                              │
       └→ F1(含单测)─→ F2(含单测)→ F3 ─→ F4 ─→ F5 ─→ F6 ─→ F7 ─→ F8 ─→ F9
                                                                          │
                                                                          ├→ T1 ─→ T2 ─→ T3 ─→ T5 ─→ T4
                                                                          │
                                                                          └→ V1 ─→ V2 ─→ V3 ─→ V4 ─→ V5
```

**关键变更**(plan-eng-review 修法):
- F1/F2 完成判定**含同步单测**(ADR-21),不延后到 T1 — 避免 401 死循环 bug 污染 F3-F8 调试
- I4a(nginx 反代)是 V5 prod 预演的前置 — 不可跳
- T5(Playwright e2e)在 T4(/cso)之前跑,e2e 暴露的问题给 /cso 评估

---

## 3. 回滚边界

- **代码回滚**:`web/` 独立目录,`git revert <PR-3-merge-commit>` 或 `rm -rf web/` 回到 PR-1 状态
- **数据回滚**:无 schema 改动(只消费 PR-1 endpoint),无回滚需求
- **部署回滚**:`docker compose up -d web:<previous-tag>` 切回上一镜像;backend 不受影响
- **PR-3 本身不部署生产**:dev + 本机 prod 预演通过即合主分支,实际生产部署等 PR-4 微信登录就绪一并上(避免裸 dev login endpoint 出现在生产网络)

---

## 4. 工程评审结论(plan-eng-review 2026-05-28)

**初次评分**(0-10):
- 架构 8 / 代码质量 7 / 测试覆盖 6 / 性能 7 / Step 0 scope 7

**收敛后评分**(全 ≥ 9):
- 架构 9(+SWR cache / +NEXT_PUBLIC_AUTH_MODE / +Modal+Toast 集中管理)
- 代码质量 9(+F1/F2 测试随写)
- 测试覆盖 9(+T1 补 4 path / +T5 Playwright 5 case)
- 性能 9(+reorder debounce 300ms)
- 部署可行性 9(+I4a nginx / +Dockerfile 3 处 COPY / +V5 5 子判定)

**Outside Voice**(Claude subagent,Codex 不可用):
- 找到 9 个前面评审错过的盲点
- 全接受 A/B/C/D/F/G/H/I,Issue E 战略错位保留阶段 0 用户决定不重做(cross-model tension)

**采纳 ADR 净增**:ADR-2 翻转 + ADR-14 增强 + ADR-15/16/17/18/19/20/21/22 新增,共 9 个 ADR 操作。

**仍 deferred 进 § 6**:axe-core 自动化 / 暗色模式 / Web Vitals / 跨项目搜索 / WebSocket / SSE / 编辑/重排乐观更新扩展 / dev login → 微信扫码 / Web 端语音 / CI/CD pipeline(GitHub Actions / image registry)

**估时调整**:4 天 → **5-6 天**

---

## 5. 时间线

| 日期 | 里程碑 | 状态 |
|---|---|---|
| 2026-05-28 | 阶段 0 / 1 / 2 设计评审完成,9.1/10 | ✅ |
| 2026-05-28 | 阶段 2.5 第二层 PR-3 任务清单定稿 | 🔄 |
| 待定 | 阶段 3 实施完成(I1-F9)+ `/review` PASS | ⏸ |
| 待定 | 阶段 4 测试完成(T1-T4)+ `/cso` 0 P0/P1 | ⏸ |
| 待定 | 阶段 5 真实浏览器联调(V1-V5)| ⏸ |
| 待定 | 阶段 7 merge 前 `/review` + `/cso` 双跑 | ⏸ |
| 待定 | 阶段 9 归档 | ⏸ |

---

## 6. 后续优化清单(实现过程中陆续追加)

**已知 deferred**:

- [ ] **短期**:axe-core 自动化 a11y 测试(ADR-12 defer 到下个 PR)
- [ ] **短期**:暗色模式(`@theme` 加 `[data-theme='dark']` 块即可,留下个 PR)
- [ ] **短期**:Web Vitals 监控接入(`useReportWebVitals`)
- [ ] **中期**:跨项目搜索 / 标签(父 feature 远期 PR-7)
- [ ] **中期**:实时推送(WebSocket / SSE,数据冲突场景出现时再考虑)
- [ ] **中期**:乐观更新策略扩展到编辑 / 重排(目前仅 IdeaInput 插入,见 ADR-11)
- [ ] **长期**:替换 dev login 为微信扫码(PR-4)
- [ ] **长期**:语音输入 navigator.mediaDevices + 后端 ASR(网页端,远期)

**阶段 5 联调可能发现并回流的(占位)**:

- [ ] `_UI设计规范.md` 辅助文字色 `#86909C` 对比度 3.8:1,Lighthouse 可能给低分 → 若真低分则 token 微调到 `#6B7280`(对比度 4.5:1)+ 回流 _UI设计规范
- [ ] PR-1 后端 `/api/projects/{id}/ideas/reorder` 已加前端 debounce 300ms,但若后端发现并发安全问题(用户同时多端拖拽)需加 PR-1 hotfix 行级锁

**plan-eng-review 2026-05-28 显式 deferred 项**:

- [ ] **CI/CD pipeline**:GitHub Actions build + push image registry,PR-3 仅 Dockerfile 就绪不接 CI(下个 PR 或 PR-4 一并)
- [ ] **暗色模式**:`@theme [data-theme='dark']` 块,留下个 PR
- [ ] **Web Vitals 监控**:`useReportWebVitals` 接入,留下个 PR
- [ ] **axe-core 自动化 a11y**:ADR-12 已 defer
- [ ] **乐观更新扩展到编辑 / 重排**:目前仅 IdeaInput 插入 + Reorder,ADR-11 例外限制
- [ ] **WebSocket / SSE 实时推送**:多端冲突场景出现时再上
- [ ] **dev login → 微信扫码**:PR-4 切换 ADR-16 mode `wechat`
