# 01-想法记录MVP · PR-3 迭代日志

> 时间倒序。

---

## 2026-05-28 · PR-3 完工 · v0.2.0 网页端 + 拖拽 + 动画系统 + nginx 反代

### 范围

- **前端**:`web/` 完整 Next.js 16 + TS + Tailwind 4 项目(20 ts/tsx + 39 单测)
- **鉴权**:AuthGate + lib/auth.ts(AUTH_MODE 三档 + idle 30min + reauth singleton)
- **API 客户端**:lib/api.ts(401 一次性 re-login + same-origin/include 自适应)
- **数据 hooks**:useProjects / useIdeas(SWR + 乐观更新)
- **UI 组件 12 个**:ProjectSidebar / IdeaInput / IdeaItem / IdeaList / SortableIdeaList / EditIdeaModal / RenameModal / ConfirmModal / ToastProvider / ModalRoot / EmptyState / Skeleton
- **拖拽**:@dnd-kit 整行可拖 + 立即本地顺序 + debounce 300ms API
- **动画**:framer-motion(Toast 滑入 / Modal 弹入 / SVG 浮动 / 完成对勾 backOut)
- **部署**:后端 Dockerfile + web/Dockerfile + docker-compose.prod.yml + nginx 反代
- **PR-1 hotfix**:CorsConfig bean name + allowedOriginPatterns / PG 端口 5432→25432 同步

### 关键决策(ADR 净增 14 个,详 01-设计.md § 4.5)

- ADR-2 翻转:引入 SWR(替代纯 useState/useEffect)
- ADR-6 翻转:做拖拽(@dnd-kit;不像原本 ADR 那样不做)
- ADR-10 删除走 toast undo(5s 软删除)
- ADR-11 IdeaInput 乐观更新(破 ADR-2 仅此一处)
- ADR-13 双击编辑 / ADR-14 双 SortableContext / ADR-15 SWR cache key
- ADR-16 NEXT_PUBLIC_AUTH_MODE 三档 / ADR-17 reorder debounce 300ms
- ADR-18 XSS 缓解四件套(CSP / ESLint no-danger / textContent / idle 30min)
- ADR-19 ModalRoot Portal / ADR-20 ToastProvider Context
- ADR-21 F1/F2 测试随写 / ADR-22 Noto Sans SC self-host

### 测试结果

| 类型 | 数量 | 结果 |
|---|---|---|
| 单元测试(api + auth + IdeaInput + ToastProvider + SortableIdeaList) | 39 | ✅ 38 pass / 1 skip(framer-motion exit jsdom 不可测,e2e 已验证) |
| `/review` BLOCKING(阶段 3) | — | ✅ 4 P0 + 11 P1 + 1 P2 + 1 P3 全修 |
| `/cso` daily 8/10 gate(阶段 4)| — | ✅ 0 P0 / 0 P1 |
| Claude in Chrome 真浏览器联调(阶段 5)| — | ✅ 金路径 13 项 + 边界全过 + 0 console error |
| 阶段 7 双门简化重跑 | — | ✅ 无新 P0/P1 |

### 发现并修复的 P0/P1

| # | 现象 | 根因 | 沉淀 |
|---|---|---|---|
| /review 暴露 | 4 P0 + 11 P1 | 见 04-部署日志 § 5 | PR-3 代码 |
| 联调暴露 | CorsConfig bean name | Spring Security CorsConfigurer 优先按 name 查找 | `04-部署日志.md` 坑 1 + 回流 PR-1 |
| 联调暴露 | 端口 5432 占用 | 多项目机器冲突 | `04-部署日志.md` 坑 2 |
| 用户反馈 | 拖拽动画"跳" | framer-motion `layout` × dnd-kit transform 冲突 | `04-部署日志.md` 坑 3 |
| 用户反馈 | 拖完回弹 300ms 跳 | scheduleReorder 把乐观更新一起 debounce | `04-部署日志.md` 坑 4 |

### 用户反馈迭代轨迹

1. v0.1.0 初版 → "页面太丑像 API 文档 + 拖拽要整行"
2. v0.1.1 UI 升级 → "拖动没有动画效果"
3. v0.1.2 加 framer-motion → "拖完先回到原位等接口才换"
4. v0.2.0 拆 onLocalReorder + onApiReorder ✅ ship

### 数据 / 效果

- web/ image size ≤ 200MB(约定,实际待 prod 预演 V5 验证)
- Lighthouse a11y 目标 ≥ 90(待 prod 预演验证)
- 桌面 ≥ 1024px 双栏布局 + 移动 < 768px 单栏布局正常
- console 0 error / network 0 4xx-5xx

### 关联 commit

| commit | 一句话 |
|---|---|
| `561de9a` | feat(PR-3): 网页端 Next.js 16 + 拖拽 + 动画系统 + nginx 反代(v0.2.0) |
| `fcb6124` | Merge PR-3 into main: 网页端 + 拖拽 + 动画系统 + nginx 反代 (v0.2.0) |

### 后续待办(02-开发计划.md § 6 列表)

- [ ] axe-core 自动化 a11y(ADR-12 defer)
- [ ] 暗色模式(`@theme [data-theme='dark']` 块)
- [ ] Web Vitals 监控(useReportWebVitals)
- [ ] dev login → 微信扫码(PR-4 切 AUTH_MODE=wechat)
- [ ] postcss CVE GHSA-qx2v-qp2m-jg93(build-time only,不阻塞)
- [ ] 删除时卡片视觉反馈(实施 deviation,ADR-10 设计的"卡片淡出 + 高度收缩"暂未实现)
- [ ] CI/CD pipeline GitHub Actions
- [ ] 跨项目搜索 / 标签(父 feature PR-7)

<!-- 新的迭代追加到上面,保持时间倒序 -->
