# 0X-功能名 · 设计

> 标准章节：需求 → 调研 → UI 设计 → 技术设计。
> 对应 SOP 阶段 1 / 2 / 3 / 4。
> 写完每节后请用户评审通过再进入下一节。

---

## 1. 需求（对应 SOP 阶段 1）

### 1.1 做什么
（一句话）

### 1.2 为什么 / 用户价值
（解决谁的什么问题，不解决会怎样）

### 1.3 不做什么（scope 边界）
- 不做 A，因为 ...
- 不做 B，因为 ...

### 1.4 成功指标
（怎么算成功，量化）

---

## 2. 调研（对应 SOP 阶段 2）

### 2.1 ctx7 查到的关键 API / 用法

### 2.2 firecrawl 查到的行业 / 竞品做法

### 2.3 项目内既有相似模式
（grep 出来的可复用 Controller / Service / 组件）

### 2.4 关键不确定性 & 假设

---

## 3. UI 设计（对应 SOP 阶段 3，无前端可删此章）

### 3.1 设计依据
- 沿用项目 UI 规范：`docs/00-项目初始上线/01-设计.md` § UI 设计规范
- 本功能新增设计 token / 组件：（若有）

### 3.2 线框图 / 原型
- `资源/线框图/XX.html`（如有）

### 3.3 响应式 / 暗色模式 / 加载&错误&空状态规划

### 3.4 文案规范
- 注意：所有可能进入 HTTP header / URL 的文案预先英文化
- 不暴露内部 pipeline（"AI 精选" → "soso 精选"）

---

## 4. 技术设计（对应 SOP 阶段 4）

### 4.1 总览架构图
（mermaid 或 ASCII）

### 4.2 数据模型
- 表 `xxx`：字段 / 索引 / 中文 COMMENT
- Flyway migration 编号 `V<N>__xxx.sql`

### 4.3 接口契约
| 方法 | 路径 | 入参 | 出参 | 说明 |
|---|---|---|---|---|

### 4.4 关键算法 / 关键决策（ADR）
- **决策 1**：选 A 不选 B，因为 ...
- **决策 2**：...

### 4.5 与既有功能的耦合点
- 依赖 X 功能的 Y 模块（已在 `docs/0X-X 功能/01-设计.md` 定义，此处仅引用）
- 影响 Z 功能：需同步更新 ...

### 4.6 迁移 / 兼容策略
（如改动既有数据/接口）

### 4.7 风险与回滚边界
（出问题能 5 分钟回滚到什么状态）
